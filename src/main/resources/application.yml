server:
  port: 8333

spring:
  main:
    banner-mode: "off"
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:purser
    username: admin
    password: admin
  h2:
    console:
      enabled: true
  jpa:
    #show-sql: true
    #properties.hibernate.format_sql: true

swagger:
  title: Purser
  version: 1.0